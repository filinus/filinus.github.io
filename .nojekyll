Why .nojekyll
From  https://docs.github.com/en/pages/quickstart 
"...Alternatively, if GitHub Actions is unavailable or disabled, 
adding a .nojekyll file to the root of your source branch will bypass the Jekyll build process 
and deploy the content directly..."
